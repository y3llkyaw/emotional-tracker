import 'package:flutter/material.dart';

class EmojisSheet extends StatelessWidget {
  const EmojisSheet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
